# allow crawling everything by default (search engines + AI crawlers)
User-agent: *
Disallow:

# block Google AI training only (Google-Extended); googlebot search crawl stays allowed
User-agent: Google-Extended
Disallow: /

